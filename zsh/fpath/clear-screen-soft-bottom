# put cursor at bottom of terminal

emulate -L zsh

[[ -t 1 ]] || return
zmodload zsh/terminfo
echoti civis
print -rn -- "${(pl:$((2 * LINES - 1))::\n:)}"
zle -I
zle -R
print -rn -- ${${terminfo[cnorm]-}:/*$'\e[?25h'(|'\e'*)/$'\e[?25h'}

# vim: ft=zsh
